# ==============================================================================
# AXOIQ SYNAPSE - Environment Configuration Example
# ==============================================================================
# Copy this file to .env and configure for your environment
#
# SECURITY WARNING: Never commit .env files to version control!
# SECURITY WARNING: Change all default values in production!
#
# For local development: Use PostgreSQL via Docker on port 5433
# For production: Configure with production PostgreSQL credentials
# ==============================================================================

# ------------------------------------------------------------------------------
# ENVIRONMENT
# ------------------------------------------------------------------------------
# Options: development, test, production
ENVIRONMENT=development

# ------------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ------------------------------------------------------------------------------
# PostgreSQL (Recommended for development and production)
# Start PostgreSQL: docker-compose up db -d
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/synapse

# Analytics / Owner DB (read-only)
ANALYTICS_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/synapse_analytics

# SQLite (Quick prototyping only - NOT recommended for production)
# DATABASE_URL=sqlite:///./synapse.db

# ------------------------------------------------------------------------------
# AUTHENTICATION & SECURITY
# ------------------------------------------------------------------------------
# CRITICAL: Change this in production!
# Generate with: openssl rand -hex 32
#
# WARNING: The default value is for DEVELOPMENT ONLY!
# Using the default in production is a SECURITY VULNERABILITY!
SECRET_KEY=dev-only-change-in-production

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ------------------------------------------------------------------------------
# CORS CONFIGURATION (Production Only)
# ------------------------------------------------------------------------------
# Comma-separated list of allowed origins for production
# Example: ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
#
# Leave empty for development (uses localhost defaults)
ALLOWED_ORIGINS=

# ------------------------------------------------------------------------------
# OPTIONAL: Advanced PostgreSQL Configuration
# ------------------------------------------------------------------------------
# Only needed if not using DATABASE_URL
# POSTGRES_SERVER=localhost
# POSTGRES_PORT=5433
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=synapse

# ------------------------------------------------------------------------------
# OPTIONAL: Redis Configuration
# ------------------------------------------------------------------------------
# REDIS_URL=redis://localhost:6379/0

# ------------------------------------------------------------------------------
# OPTIONAL: Logging Configuration
# ------------------------------------------------------------------------------
# LOG_LEVEL=INFO
# LOG_FORMAT=json
