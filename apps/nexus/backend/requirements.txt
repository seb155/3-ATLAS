# ==============================================================================
# NEXUS BACKEND DEPENDENCIES
# Workspace Integration Support (shared auth, Redis, PostgreSQL)
# ==============================================================================

# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9

# Redis (workspace shared cache)
redis==5.0.1
hiredis==2.3.2  # Performance boost for Redis

# Authentication & Security (workspace SSO)
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.0.1  # Pin version for passlib compatibility
python-multipart==0.0.6  # Form data support

# Validation
pydantic==2.6.0
pydantic-settings==2.1.0
email-validator==2.1.0  # Email validation for Pydantic

# Utilities
python-dotenv==1.0.0
html2text==2024.2.26

# Trilium Integration
trilium-py==0.9.1  # TriliumNext ETAPI client
aiohttp==3.9.3  # Async HTTP for Trilium sync

# Production Server (optional)
gunicorn==21.2.0  # Production WSGI server

# Development & Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
pytest-cov==4.1.0  # Code coverage
