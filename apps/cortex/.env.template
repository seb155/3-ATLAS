# CORTEX Environment Configuration
# This file is a template - DO NOT edit directly
# Run: .\generate-env.ps1 to create .env with correct values
#
# Auto-replaced placeholders:
# - {{DATABASE_HOST}} -> forge-postgres (Docker) or localhost (Local)
# - {{DATABASE_PORT}} -> 5432 (Docker) or 5433 (Local)
# - {{REDIS_HOST}} -> forge-redis (Docker) or localhost (Local)
# - {{REDIS_PORT}} -> 6379 (both)
# - {{ENV_MODE}} -> docker or local

# ===================
# Environment
# ===================
ENVIRONMENT={{ENV_MODE}}
DEBUG=true

# ===================
# Database
# ===================
DATABASE_URL=postgresql://postgres:postgres@{{DATABASE_HOST}}:{{DATABASE_PORT}}/cortex

# ===================
# Cache
# ===================
# Using database 2 for CORTEX (database 0 for SYNAPSE, 1 for NEXUS)
REDIS_URL=redis://{{REDIS_HOST}}:{{REDIS_PORT}}/2

# ===================
# AI Providers
# ===================
# Primary provider (required)
# MANUAL: Add your actual API key
ANTHROPIC_API_KEY=sk-ant-xxx

# Alternative providers (optional)
# MANUAL: Add your actual API keys if needed
OPENAI_API_KEY=sk-xxx
GEMINI_API_KEY=xxx

# ===================
# External Services (ai-sandbox)
# ===================
# These use Docker DNS names when running in Docker
# For local development, update these to localhost URLs
LITELLM_URL=http://litellm:4000
OLLAMA_URL=http://ollama:11434
CHROMADB_URL=http://chromadb:8000
LANGFUSE_URL=http://langfuse-server:3000

# ===================
# Context Settings
# ===================
MAX_CONTEXT_TOKENS=100000
HOT_CACHE_SIZE=50000
WARM_CACHE_SIZE=100000

# ===================
# Model Defaults
# ===================
DEFAULT_MODEL=claude-sonnet-4-20250514
EMBEDDING_MODEL=text-embedding-3-small

# ===================
# Security
# ===================
# MANUAL: Change this in production!
SECRET_KEY=change-this-in-production

# CORS origins (comma-separated)
# Docker mode: Use Traefik domains
# Local mode: Use localhost URLs
CORS_ORIGINS=http://localhost:5173,http://localhost:4000,https://cortex.axoiq.com
