{
  "$schema": "dev-manifest-schema",
  "version": "1.0",
  "context_type": "standalone",
  "project": {
    "id": "{{PROJECT_ID}}",
    "name": "{{PROJECT_NAME}}",
    "path": "{{PROJECT_PATH}}"
  },
  "metadata": {
    "created": "{{TIMESTAMP}}",
    "last_updated": "{{TIMESTAMP}}"
  }
}
